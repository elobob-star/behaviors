# Canary Prompt

> Used by the daily canary probe in `Orchestrator.run_canary()` to
> detect drift in Jules' response shapes (vision §6). Tiny, cheeky,
> sandbox-safe.

Canary task: append the current date as a single line to CANARY.md
(create the file if missing). Make no other changes.
